| Pattern | Purpose |
|---------|---------|
| Dark gradient background | Creates modern, immersive feel with `linear-gradient(135deg, #1a1a1a, #2d2d2d)` |
| `.feature-card` with cyan accents | Uses `#64c8ff` / `#4ba3d9` for visual distinction |
| `.info-card-coming-soon` | Centered placeholder for future features |
| `::before` pseudo-element | Animated top border that slides in on hover |
| `translateY` hover effect | Indicates clickability on interactive elements |

---

### Step 902.5: Register in AppModule

The AutomobileComponent is declared directly in the root `AppModule` rather than using a separate feature module. This approach provides simpler setup for eagerly loaded components.

In `src/app/app.module.ts`, ensure AutomobileComponent is imported and declared:

```typescript
// In src/app/app.module.ts
import { AutomobileComponent } from './features/automobile/automobile.component';

@NgModule({
  declarations: [
    AppComponent,
    HomeComponent,
    AutomobileComponent,
    // ... other components
  ],
  imports: [
    BrowserModule,
    BrowserAnimationsModule,
    HttpClientModule,
    // ... other modules
  ],
  // ...
})
export class AppModule {}
```

---

## Verification

### 1. Check File Structure

```bash
$ find src/app/features/automobile -type f | sort
```

Expected output:

```
src/app/features/automobile/automobile.component.html
src/app/features/automobile/automobile.component.scss
src/app/features/automobile/automobile.component.ts
```

### 2. Build the Application

```bash
$ ng build
```

Expected: Build succeeds with no errors.

### 3. Visual Verification (After Routing)

Once routing is configured (document 905), navigate to `http://localhost:4200/automobiles`:

- Section header with car emoji: "Explore Automobile Data"
- Four feature cards in a responsive grid with dark theme
- "Advanced Search" card has cyan accent and links to discover page
- Three info cards at the bottom with dark gradient backgrounds
- "Coming Soon" card placeholder for future features

**Figure 902.1** shows the Automobile landing page with its feature cards:

![Automobile landing page with feature cards](../e2e/screenshots/V1.1.2-filter-panel-default.png)

*Figure 902.1: The Automobile landing page. The "Advanced Search" card is prominently styled with cyan accent colors and navigates to the discover page where users can search and filter vehicle data.*

**Figure 902.2** shows the Discover page with the full data exploration interface including filters, results table, and statistics:

![Discover page showing filter panel, results table, and statistics](../e2e/screenshots/V1.1.1-results-table-default.png)

*Figure 902.2: The AutomobileDiscoverComponent showing the three main panels: filter panel (left), results table (center), and statistics panel (right). This layout enables users to iteratively refine their searches while viewing live results.*
