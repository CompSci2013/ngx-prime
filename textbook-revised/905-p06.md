| Feature | Purpose |
|---------|---------|
| `*ngIf="!isPopOut"` | Hides header in pop-out windows |
| `routerLinkActive="active"` | Highlights current route in navigation |
| `[class.popout-content]` | Removes padding for pop-out content |
| Route change detection | Updates `isPopOut` on navigation |

---

### Step 905.5: Router Configuration

The `RouterModule.forRoot()` configuration uses defaults:

```typescript
RouterModule.forRoot(routes)
```

No additional options are configured. The URL-First architecture is implemented through the `UrlStateService` and `ResourceManagementService` rather than router options.

---

## Verification

### 1. Build the Application

```bash
$ ng build
```

Expected: Build succeeds with no errors.

### 2. Serve and Navigate

```bash
$ ng serve --open
```

Test these navigation paths:

| Action | Expected Result |
|--------|-----------------|
| Open `http://localhost:4200` | Redirects to `/home` |
| Click "Automobiles" card | Navigates to `/discover` |
| Enter unknown URL like `/foo` | Redirects to `/home` |

### 3. Verify Component Loading

Open browser DevTools (Network tab) and observe:

1. Initial load: All components are bundled together (eager loading)
2. No additional network requests on navigation between routes

### 4. Verify Query Param Persistence

1. Navigate to `/discover?manufacturer=Toyota`
2. Note the query parameter in URL
3. Refresh the page
4. Query parameter should persist (handled by UrlStateService)
